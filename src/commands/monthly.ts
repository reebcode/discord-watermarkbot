import { Client, Message, TextChannel } from 'discord.js'

// name: 'clear',
// description: "Use this command to clear channels of users who left the server.",

export async function monthly(message: Message, args: string[], client: Client) {
    if (message.member?.roles && message.member?.roles.highest.id == '786628196002037770') {
        
    }
}
